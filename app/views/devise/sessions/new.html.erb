<div class="d-flex justify-content-center">
  <h2 class="display-6">Log in</h2>
</div>


<div class="d-flex justify-content-center">
  <div class="card mt-3 border-0" style="width: 22rem;">
    <div class="card-body d-flex flex-column align-items-center">
      <div class="card-text text-center">
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email,
                        required: false,
                        autofocus: true,
                        label_html: { class: "d-flex justify-content-center mb-2" },
                        input_html: { autocomplete: "email", class: "mb-2" } %>
            <%= f.input :password,
                        required: false,
                        label_html: { class: "d-flex justify-content-center mb-2" },
                        input_html: { autocomplete: "current-password", class: "mb-2" } %>
            <%= f.input :remember_me, as: :boolean, input_html: { class: "mx-1" } if devise_mapping.rememberable? %>
          </div>

          <div class="form-actions mt-3">
            <%= f.button :submit, "Log in", class: "btn btn-outline-primary mb-2" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
